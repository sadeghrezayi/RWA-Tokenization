# KNOWN ISSUES

Every issue established from the code, the test runs, or the development history. Issues that were
**found and fixed** are kept at the bottom, because their causes are traps a future session could
walk back into.

---

## Open issues

| # | Severity | Symptom | Likely cause | Component | Workaround | Next step |
|---|---|---|---|---|---|---|
| K-1 | ~~High~~ **FIXED** 2026-08-17 | Issuer team management had no browser flow | Delivered in slices; the review queue landed first (3.2f), the team panel second (3.2g) | `apps/web/components/admin/issuer-detail-page.tsx` | — | Remaining: the **issuer-facing** portal, where an issuer's own admin manages their team without staff (roadmap 3.3) |
| K-17 | **Medium** | Every server-rendered page returns **500** with `SyntaxError: Unexpected non-whitespace character after JSON at position 979`, including `/favicon.ico`. Client-rendered admin pages still work, which makes it look like a routing bug | **Running `pnpm build` in `apps/web` while `next dev` is serving.** Both write the same `apps/web/.next`, and the production build leaves the dev server holding artifacts it did not write — `prerender-manifest.json` ends with duplicated bytes (`…b53c95c38"}}b53c95c38"}}`). Not application code. Observed 2026-08-16, and again 2026-08-18 after building to satisfy the CI gate while the preview server was up | `apps/web/.next` (git-ignored) | — | Stop the dev server, `rm -rf apps/web/.next`, start it again — takes under a minute and the first page compiles in ~3s. **Avoid it** by stopping the web preview before running the production build. The error message names nothing, so check this **first** when every route 500s at once **A SECOND SYMPTOM of the same cause** (2026-08-23, filed separately as K-36 before the duplication was spotted): the dev server can instead serve `Cannot find module './<chunk>.js'` and flood the console with "Failed to fetch RSC payload", because the production build replaced the chunks the running dev server still expects. Both symptoms, one cause. **Verified remedy 2026-08-24** — order matters: stop the dev server, WAIT for the process to actually exit, THEN `rm -rf apps/web/.next`, then start it again. Clearing while the old server is still shutting down lets it rewrite the directory and `prerender-manifest.json` comes back corrupt (961 bytes, valid JSON to char 957, then a duplicated `d"}}` tail — exactly the position the error names). Diagnose it by parsing the JSON files under `.next` rather than reading the stack: `json.loads` names the file in one step. |
| K-18 | **Medium** | `npx playwright install` fails with **403 "this service is not available in your location"** from `cdn.playwright.dev`, so the bundled Chromium cannot be downloaded on this machine | Geographic block on Playwright's CDN | Playwright | Run the suite against the **system Chrome**: leave `PLAYWRIGHT_CHANNEL` unset (the config defaults to the `chrome` channel) instead of setting it to `""`. CI sets it empty on purpose because the runner *does* have the bundled browser | Keep both paths working; do not "simplify" the channel logic in `playwright.config.ts` |
| K-20 | **Low** | One integration test failed once on 2026-08-17 and did not reproduce in three subsequent runs, including under the same chained build-then-test conditions | **Unknown.** The failing test's name was not captured. A plausible but UNPROVEN mechanism: the API preview server was running against the same Postgres while the suite ran, so live demo data and suite data shared tables. **See K-21** — host sleep is now a confirmed cause of exactly this shape of one-off failure | integration suite | — | If it recurs, capture the test name before re-running. Prefer stopping preview servers before a full integration run |
| K-21 | **Medium** (environment, not code) | Long local runs report **"Test timed out in 30000ms"** on the anvil-backed integration tests, and vitest prints impossible durations — a single test file reporting `1008.34s` while its 30s timeout "fired", and a whole suite reporting `Duration 3602.00s` where it normally takes ~194s | **The development Mac sleeps mid-run** (`pmset -g log` showed `SleepService: window has terminated` and 927 sleep/wakes). Wall-clock stalls starve vitest's timers, so tests that wait on chain I/O are killed by a timeout that was never really 30 seconds of work. Confirmed 2026-08-18: anvil answered `eth_blockNumber` instantly and mined 195 blocks during the stalls, and the Postgres-backed files ran 57 tests in 5.79s in the same session | local integration suite (`services/api`, `test/integration/trex-*.test.ts`) | Compare the printed `Duration` against `date` before and after. If they disagree, the run is void — do not file the failures as defects. Run the DB-backed files by name for a fast, trustworthy signal | Treat **CI as the authority** for the chain tests; the runner does not sleep. `caffeinate -dimsu` was tried and did **not** hold on this host, so do not assume it fixes the run |
| K-22 | **Medium** (CI) | A CI run ends as **`cancelled` after exactly ~25 minutes**, stuck on the `Install Playwright browser` step, while every neighbouring run finishes in ~5 minutes. The run reports no failing test, so it reads like a mystery | The step (`playwright install --with-deps chromium`) hung on the download; the job then hit the workflow's `timeout-minutes: 25` and GitHub reported the whole run as cancelled rather than failed. Observed on commit `1caaa68` (2026-08-18) — the same tree passed on a re-run | `.github/workflows/ci.yml` line 126 | Re-run the workflow; the code is not implicated. Confirm by checking whether ANY test step reported a failure before the cancellation | **Done 2026-08-18:** `timeout-minutes: 8`, so a hung download fails fast and names itself. **Recurred twice on 2026-08-19** — once erroring outright, then hanging through all three retries until the 8-minute bound. Three failed builds in two days, none of them the code's fault. **Resolved by not downloading a browser at all**: the runner image ships Google Chrome and the suite drives it through the `chrome` channel, the same way the developer machine already does (where the CDN is blocked outright). Trade-off recorded in the workflow: CI's browser now moves with the runner image rather than the lockfile — acceptable because these are assertion-based contracts with no pixel baselines, and it removes the CI/local browser difference that cost a long investigation |
| K-23 | **Medium** (environment, not code) | The anvil-backed integration suites time out in `beforeAll` — 30s, 60s and 90s hooks all blown — while every Postgres-backed file passes in milliseconds and the same tests pass in CI | **A long-lived anvil degrades.** Measured 2026-08-18 with the identical one-contract deploy against two nodes on the same machine: **101 ms on a fresh anvil, 4121 ms on one carrying 2119 blocks**. Reads (`eth_blockNumber`, `getTransactionCount`) stayed at 12–20 ms on both, so the node looks perfectly healthy — only the receipt wait collapses, and a T-REX suite deployment is dozens of transactions | local anvil devnet (`:8545`) | Restart anvil. Nothing in Postgres depends on chain state unless an asset is **tokenized** — check `Asset.tokenAddress` first, and if any is set, reset both together (K-16) | **This also reached CI** (run on `3d92019`): the browser journey ran on the anvil the integration suite had just exhausted, and died inside `POST /assets/:id/tokenize` on the 240-second test timeout with no failing assertion to point at. Fixed 2026-08-18 by restarting the devnet and redeploying before that step. Proven by holding everything else constant — same commit, same database, same servers, aged node vs fresh: **timeout vs 3 journeys in 45 s**. Diagnose by timing, not by reading  **Confirmed again 2026-08-25, and it is entirely the aged node.** All four anvil suites failed locally against the long-running devnet, then all four passed — 9 tests, under 8 seconds total — against one started fresh seconds earlier. The whole local cure is `pkill -f anvil` then `anvil --host 127.0.0.1 --port 8545 --silent`, and nothing about the code changes. Earlier sessions reported these suites as 'unverifiable locally'; that was overstated — they are verifiable, on a fresh node. |
| K-24 | **Low** (test timing, not a defect in the subject) | The Phase-2 exit journey fails intermittently on `expect(holdingLink).toBeVisible()` right after an offering closes — "element(s) not found" — while the same holding is present in `GET /portfolio/me` and rendered as a link the moment anyone looks afterwards. Reproduced on 2026-08-18, then passed on an immediate re-run with everything held constant | The holding is read from the **chain** (`GetMyHoldings` → `balanceOf`), so the assertion waits on a devnet round trip plus navigation and render, against Playwright's 5-second default. The close itself awaits the mint receipt (`await (await token.mint(...)).wait()`), so the balance is already final — nothing is racing in the product, only in the assertion's patience | `apps/web/e2e/journey.spec.ts` | — | Fixed 2026-08-18 by giving that one assertion a 30-second timeout with the reason written next to it. **Do not "fix" this class by widening every timeout** — check first whether the subject is actually correct, as it was here |
| K-25 | **Medium** (process) | A CI-only failure cannot be diagnosed from outside: `GET /actions/jobs/{id}/logs` returns **403** and artifact download returns **401** without a token, and step summaries are not exposed by the API at all. Between 2026-08-18 runs `3d92019` and `f2689b5` this cost **three wrong fixes**, each aimed at a cause inferred from a local reproduction that did not match CI | Public-repo read access covers runs, jobs and steps — but not their output | `.github/workflows/ci.yml` | Read the **step durations** first: all four failures ran 31 s, far too short for the 240 s timeout being theorised, which alone falsified two of the three fixes | Fixed 2026-08-18: Playwright writes a JSON report and the workflow publishes each failing spec's title and first error line as a **commit status**, which IS readable anonymously (`GET /repos/{o}/{r}/commits/{sha}/statuses`) |
| K-26 | **Medium** | A failed request could reach the screen as an error box containing **nothing** — the exit journey caught the portfolio page rendering `role="alert"` with empty text, telling the reader something broke and not one thing more | `body.message ?? res.statusText` in the web API client: `??` does not catch an empty string, and `statusText` is empty over HTTP/2. Every screen in both portals uses this client, so the blank box could appear anywhere | `apps/web/lib/api.ts` | — | Fixed 2026-08-18: the message falls through message → statusText → the status code itself, which is worth more than silence. **An empty alert is still being rendered somewhere on the portfolio page** (`EMPTY-ALERTx1` in the journey's diagnostic even on a healthy render) — with this fix that failure will now carry words, and naming it is the next step |
| K-27 | **HIGH** | A signed-in person is shown the **sign-in panel** while their session cookie is valid. Found in CI after five days of chasing it: the holder's portfolio rendered "Investor Access — sign in" because `GET /auth/session` answered **429**, with `csrf-cookie=true` | The auth rate limiter counts **every** route on the auth controller, including the credential-free session read that **every page load of every portal performs**, against one budget of **20 per minute per IP**. A single active person browsing twenty pages in a minute locks themselves out; behind a shared address — CGNAT is the norm in the target market — one budget covers **everyone at once**. The web hook then treated 429 as "not signed in", inviting a re-login that spends the same budget | `http/rate-limit.guard.ts`, `lib/use-browser-session.ts` | — | Fixed 2026-08-18, both halves: safe reads get their own ceiling (300/min, still bounded) while credential attempts keep 20/min, mutation-checked; and the hook no longer calls a rate-limited reader signed out. **Worth revisiting with the owner**: 20/min for credential endpoints is also shared per IP, so a CGNAT population still shares one login budget (K-9 — the limiter is per-process and in-memory) |
| K-28 | ~~Medium~~ **FIXED** 2026-08-19 | The issuer portal's header was **unstyled from the day it shipped** (3.3e) — no border, no height, no padding — and the notification bell overflowed a box it was never given | The shell used class names that do not exist in `components.css`: `topbar`, `topbar__actions`, `content`. The design system's are `shell__topbar` and `shell__content`. Nothing failed, because a missing CSS class is silent — the portal simply looked wrong, and only on a surface that had no layout contract | `components/issuer/issuer-shell.tsx` | — | Fixed by using the real names. Found by giving the issuer portal its first layout contract (the other two surfaces have had them since 2.6). **The class is now closed for every surface**: `test/class-names.test.ts` asserts that every class a component names exists in `components.css`, and it found **five more** the moment it ran — `.form-error` twice (a second name for `.field__error`) and `.stack--tight` three times (a modifier the system implied but never defined). Mutation-checked by reintroducing `topbar__actions`, which it names |
| K-29 | **Low** | The class-name guard does not check classes assembled into a VARIABLE before they reach `className` — `Button` builds `["btn", `btn--${variant}`, block ? "btn--block" : ""]` and passes the joined string. A misspelling there still passes | The guard is anchored to `className=` and walks that expression; a class that never appears syntactically inside one is invisible to it. Closing this properly needs AST analysis, not a wider regex | `apps/web/test/class-names.test.ts` | — | Left open deliberately. **Do not attempt the dead-CSS measurement with a hand-rolled scanner**: it was tried three times on 2026-08-19 and was wrong three different ways — conditional classNames invisible (24 candidates), variable-assembled classes invisible (14), and `badge--${tone}` variants counted as unused (28). Each list looked authoritative. If dead CSS is worth removing, use a real parser |
| K-30 | ~~HIGH~~ **FIXED** 2026-08-19 | With the devnet **unreachable**, the API does not return an error — the **process dies**. Observed 2026-08-19: `node dist/main.js` logged `api listening`, then on the first chain-touching request `triggerUncaughtException` / `Error: connect ECONNREFUSED`, and the server was gone. K-2 described this as "returns 500"; a 500 is what you get from a STALE build, and the real behaviour is worse | `new JsonRpcProvider(url)` probes for the network on construction, and on failure retries **forever** in the background — 336 `failed to detect network; retry in 1s` lines in a 5-minute isolated run, keeping the event loop alive long after the call site caught its own error. Under the API's timing that rejection escaped as an uncaught exception. There are **seven** such constructions across the chain adapters, and the loops **ACCUMULATE**: measured 2026-08-19, five providers produced 11 background retries per 3s and ten produced 24 — roughly one extra retry per second, permanently, for every chain call made while the node is down. The health probe builds two per call (`chain()` and `pausedTokenCount()`) and is what the **Overview screen** calls on mount — so the screen an operator refreshes *during* an outage is the one that compounds it | `infrastructure/chain/*.ts`, `reporting/platform-health-probe.ts` | Keep the devnet up. The placeholder path (three devnet vars unset) is unaffected | **Fixed, in two parts, because the first was not enough.** (1) One `chainProvider()` factory tells every provider its network up front, so nothing probes and nothing retries — all **ten** constructions now use it (the earlier count of seven was wrong). Chain id from `DEVNET_CHAIN_ID`, default 31337. (2) That killed the retry loops but the process STILL died: the transport emits bare socket rejections with no application frame in the stack, and Node exits on those by default. `main.ts` now logs an unhandled rejection loudly and keeps serving. Verified end to end against a genuinely dead port: **503 with the full explanation, zero retry loops, zero crashes, and the API answering 200 afterwards** — where the same check previously left a corpse. CI still cannot see this path, so the verification is the local outage script, kept at `scratchpad/chaindown.sh` in the session notes |
| K-31 | **Medium** (CI flake) | The Phase-2 exit journey intermittently fails at `POST /assets/:id/tokenize` with a **500**, on a fresh devnet that the workflow restarts immediately beforehand | **Unknown, and non-deterministic**: proven by the tree, not by argument — commit `515ecb4` passed and `3491419` failed with the ONLY differences between them being `KNOWN_ISSUES.md` and `.gitignore`. No application code, no test, no workflow change | chain tokenization path | Re-run the workflow | Capture the API log for the failing run — CI writes it to the step summary, which needs admin rights to read; a commit status carrying the log's last error line would make it diagnosable the way K-25's fix made the browser failures diagnosable. **Unproven hypothesis worth testing first:** this may be K-30 wearing a different hat — if one chain call fails, the provider's background retry loop can escape as an uncaught exception, and everything after it fails oddly. **Struck again 2026-08-25 on `02539e9`** (a commit that touched only a new read-only reporting endpoint and its screen — nothing on the tokenization path), which is further evidence for the flake reading. **The recommended next step above is now DONE:** `.github/scripts/api-log-failures.mjs` extracts the API log's own ERROR lines and first stack frame, and the workflow publishes them as `api-failure-N` commit statuses beside the existing `browser-failure-N` ones. The extraction is a standalone script rather than inline YAML precisely so it can be run against a real log and checked — verified against a fixture plus missing-file, no-error, ANSI-coloured, repeated-line, more-than-four and over-long cases. **The next occurrence should therefore carry its own cause**, and if it does not, the gap is in the extraction rather than in the evidence. **Confirmed non-deterministic again, empirically:** `3d7545c` carries every line of application code from the failed `02539e9` and passed — the only additions were this diagnostic and an ESLint override, neither reachable from a request. **Caveat, stated because it matters:** the extraction is verified against fixtures, but the WORKFLOW STEP that publishes it has never run — CI passed, so it never fired. Its dynamic import and status-publishing are unproven until the next red build, and if `api-failure-N` statuses do not appear then, suspect the step wiring before suspecting the log |
| K-32 | ~~Medium~~ **FIXED** 2026-08-19 | A run reported **`cancelled`** after 25 minutes with no failing test — the job's own budget, spent by one step | The **devnet-restart step I added for K-23** was unbounded and hung for **21 minutes** (12:37→12:58 on `b7dda47`). Two ways it could: `pkill` frees port 8545 asynchronously, so a new anvil can lose the race and exit with "address in use" while the OLD one keeps answering the readiness check; and the backgrounded anvil inherited the step's stdout, which alone can hold a step open | `.github/workflows/ci.yml` | Re-run | Fixed: `timeout-minutes: 5`, wait for the port to be released before rebinding, anvil's output to its own file, and an explicit check that the restarted node answers — printing its log if not. **The lesson is K-22's, which I had already written down and then broke by adding an unbounded step to the same workflow** |
| K-33 | ~~HIGH~~ **FIXED** 2026-08-19 (`75cde21`) | **The legal dossier could not hold a real document.** The admin screen has no file input at all: attaching a title deed sends `btoa(`${title} placeholder content`)` — the literal string "<title> placeholder content" as the document's bytes. Those bytes go to IPFS, that string is what the sha256 attests, and six of them mark the dossier COMPLETE, which is what approval and tokenization require. An asset can therefore be approved and tokenized with no evidence behind it | Two causes. (1) The UI was built without a file picker — the ONLY `type="file"` in the whole web app is the investor's KYC evidence. (2) The endpoint takes base64 inside a JSON body, so it is bound by Nest's ~100 kb default; a real deed would not fit even if the UI sent one. KYC evidence already does this properly: multipart, `FileInterceptor`, `MAX_EVIDENCE_BYTES` of 10 MB | `apps/web/components/asset-detail-page.tsx:238`, `http/assets.controller.ts` | None — the stored bytes are not the document | This strikes at the platform's premise: PRD/CLAUDE.md say a token is only as good as the off-chain enforceable right behind it, and the dossier is that evidence. `IMPLEMENTATION_STATUS` lists the documents centre as **COMPLETE** with caveats that do not mention this. Fix by mirroring the evidence path (multipart + size limit) and giving both the admin and issuer screens a real file picker. Malware scanning is separately deferred to 3.4 (threat model T17) | **FIXED:** both the admin and issuer screens now have a real file picker reading the chosen file's actual bytes (`lib/file-bytes.ts`, via FileReader because jsdom lacks `File.arrayBuffer`), with a 10 MB client limit and the server body limit raised to 16 MB to carry base64's ~33% inflation. **What did NOT change:** the endpoint still takes `contentBase64` in a JSON body rather than multipart as the KYC evidence path does — a bounded difference in transport, not the defect. Verified 2026-08-22 by reading the code: no `placeholder content` remains anywhere in the repo.
| K-34 | ~~HIGH~~ **RESOLVED 2026-08-25** | **Closing an offering could take the money and not mint the tokens, with no way to retry.** `CloseOffering` saves the closed offering FIRST, then loops the allocations doing `rail.capture` and `issuer.mint`. If a mint throws, the offering is already `closed_success` and the cash already captured — and closing again is refused with `cannot close an offering in state "closed_success"`, so there is no route back | Pre-existing, but P0-2 made it likely: moving the ONCHAINID claim onto the outbox meant an approved investor could subscribe before their claim drained, and ERC-3643 rejects a mint to an unregistered holder. CI showed it as the exit journey retrying `close` for 90 s against a 409 | `application/offerings/close-offering.ts` | Ensure the claim has drained before an offering closes | **P0-2 for the claim was REVERTED** (`ea43b37`, `df4a5c3`, `45c4134`) rather than patched: the claim going async is right, but only alongside a mint path that can be retried, and the two must land together. Do that as one coherent change — enqueue the mint per allocation, make it idempotent (a redelivered mint must not double-issue), and only then move the claim. **FIXED (P0-2, four steps, complete 2026-08-25).** Step 1 made the mint idempotent per allocation (unique index on `(offering_id, investor_id)`); step 2 put a refused mint on the outbox so it retries instead of failing the close; step 4 did the same for the ONCHAINID claim, so the async claim that was reverted is back and safe. **Step 3 reversed the order**: `CloseOffering` now releases the refund, then mints, and captures ONLY after the mint confirms. The worst case is no longer 'money taken, no tokens' but 'money still held in escrow, no tokens' — wrong, but recoverable by releasing a hold rather than reversing a payment, and the queued retry settles both halves when the chain accepts. Capture is exactly-once through a unique index on `ledger_entries (investor_id, kind, reference)`, so a redelivered message cannot debit an investor twice. Mutation-checked: restoring the capture-first order fails exactly the two ordering tests and no others. **Still open:** an allocation whose mint never succeeds leaves the Rial held indefinitely. **Visibility was added the same day** — `GET /reporting/health` now returns `allocationsAwaitingMint` (how many allocations hold money for tokens that do not exist, and how much Rial in total), and the admin overview shows it whenever the count is above zero. A claimed-but-unconfirmed mint COUNTS, because that is the case most wanting a person. **The per-allocation list landed the same day**: `GET /reporting/allocations-awaiting-mint` and the **Escrow awaiting tokens** screen (REPORTING_READ, in the Reporting nav group) name whose money is held, how much, since when, and the last retry error — and distinguish `unresolved` (may already be on chain, retrying could double-issue) from `not_minted` (never attempted, safe to retry). What is still missing needs a DECISION, not code: **no automatic release** after N failed retries and **no lever to release one**. The screen deliberately has no release button — how long to wait and who may release are the owner's calls (see the 2026-08-25 entries in `open-product-decisions.md`) |
| K-35 | ~~HIGH~~ **FIXED** 2026-08-23 (`officer-4`) | **A four-eyes request made by the super_admin can never be decided by anyone.** Only `super_admin` and `approver` hold `approval.decide`; the platform seeds exactly two staff accounts — super_admin (`OFFICER_EMAIL`) and treasury (`OFFICER2_EMAIL`) — and there is **no endpoint that creates staff users**, so an `approver` account cannot exist. Self-approval is refused, correctly, which leaves such a request stuck forever | `auth/staff-bootstrap.ts` seeds two roles; `identity/authorization.ts` grants `approval.decide` to super_admin and approver only | `infrastructure/auth/staff-bootstrap.ts` | **Make the request as treasury, approve as super_admin** — that direction works and is verified end to end in a browser (2026-08-22): treasury requested, super_admin approved, distribution `paid`, holder credited 100,000 | Latent before 4.1, when it only affected rare above-threshold ledger credits. It now affects **every payout**, because every payout needs approval. The fix is an owner's call: seed a third `approver` account, grant `approval.decide` to another role, or build staff-user management (which the platform has never had). **FIXED** by seeding a fourth staff account (`OFFICER4_EMAIL`, default `approver@platform.local`) with the `approver` role — the least committal of the three options: it grants `approval.decide` to no role that lacked it, and builds no staff-management surface. Proven end to end (`staff-rbac-api.e2e`): the super-admin parks a credit, is refused **409** on approving their own request, the approver approves it **204**, and the balance moves. The approver is refused **403** on originating a credit, so a checker can never also be a maker. The same slice seeded the `auditor` account (`OFFICER3_*`) for the identical reason. **Still not built, deliberately: a staff-management surface** — creating arbitrary staff at runtime is a bigger decision than giving two known roles a login |
| K-19b | ~~Low~~ **FIXED** 2026-08-18 | A refusal in the issuer portal read `"fbf6ca8e-…" does not act for issuer organisation "00000000-…"` — the reader's own account UUID, recited back at them as the entire page | `NotIssuerTeamMemberError` took both ids and put both in the message. **Fifth instance of this class**: the reader is the person being refused, so their id tells them nothing, and the organisation's id is already in the address bar they typed | `application/issuers/errors.ts` | — | Fixed by making the error carry no ids at all. Found by LOOKING at the page in a browser, not by a test — the test suite was happy. When adding an error a human will read, open it in the UI |
| K-19 | ~~Low~~ **FIXED** 2026-08-17 | The issuer queue showed **"Decided by officer-1"** — an internal account id, not a person | `IssuerOrganisationView.decidedBy` carries only the officer id; the read model has no staff-directory lookup | `application/issuers/issuer-views.ts`, `components/admin/issuers-panel.tsx` | — | Resolve the id to a name/email in the read model, exactly as the approval alert was fixed (`fix(notifications): human labels…, not raw ids`) |
| K-2 | ~~High~~ **Medium** (partly fixed 2026-08-19; the async claim that would close it was reverted — see **K-34**) | Approving KYC failed with a bare **500** when the devnet was unreachable — and, worse, **the investor was never told about a decision that had already been committed**: the approval is deliberately persisted before the chain is touched, but the chain error aborted the notification that came after it | The ONCHAINID claim is issued **synchronously** inside the request | `application/identity/approve-kyc.ts`, `chain/onchainid-claim-issuer.ts` | Run anvil, or unset the three devnet vars to use the logging placeholder | **Fixed:** the investor is notified before the chain call, and a chain failure now raises `ClaimIssuanceFailedError` → **503**, saying the approval stands, the claim did not issue, and it can be retried. Mutation-checked both ways. **Recovery added 2026-08-19:** `POST /investors/:id/kyc/reissue-claim` (KYC_REVIEW). Until it existed there was **no way back at all** — `issueKycApprovedClaim` had exactly one caller, inside a transition that runs once, so an approval whose claim failed left the investor approved and permanently unable to hold anything (ERC-3643 refuses transfers to an unverified wallet). The earlier note here said the claim must be reissued 'by hand', which overstated what was possible: there was no hand. Refuses for anyone not already approved, so it recovers the chain half and decides nothing — mutation-checked. **Still open:** it is manual and synchronous; automatic retry is P0-2 |
| K-3 | **High** | No email ever arrives | No SMTP adapter; delivery goes to a dev sink | `application/identity/email-outbox.ts` | Read the sink | Implement a nodemailer adapter behind the existing port (P0-3) |
| K-4 | ~~Medium~~ **DIAGNOSABLE** 2026-08-19 | A published offering can stay invisible on the public site — or a **withdrawn one stay advertised** — for the whole revalidate window | `REVALIDATE_SECRET` unset, or different between the API and web. Both causes were silent: unset was a per-call `debug` line, and a mismatch logged only "returned 401" | `web-public-page-revalidator.ts`, `app/api/revalidate/route.ts` | Set the same value on both | Both causes now say so in words: unset warns **once at boot** naming the consequence, and a refused purge says the two sides disagree rather than quoting a status code. **Deliberately not a hard failure** — an API-only deployment has no public site to purge, and refusing to boot is an operational call for the owner, not an engineering one |
| K-5 | **Medium** | An onboarding application that is waiting **on the applicant** still appears in the officer's pending queue and the ops work queue | The queue counts "open cases", not "waiting on us" | `application/onboarding/*`, `application/ops/*` | — | Product decision: split the queue semantics |
| K-6 | **Medium** | A KYC-rejected applicant can never re-apply | `rejected` is a terminal state by design; resubmission is now explicitly refused rather than silently doing nothing | `domain/identity/kyc-status.ts` | — | Product decision |
| K-7 | **Medium** | CRM follow-up reminders go to **every** staff member holding `crm.manage` | A follow-up has no owner field | `domain/crm/follow-up.ts` | — | Schema + UX change if per-follow-up ownership is wanted |
| K-8 | **Medium** | An investor with an unverified or mistyped email can reach a KYC decision that cannot be delivered | Accepted consequence of OD-22 (nothing is gated on email verification) | identity | — | Revisit OD-22 if it bites |
| K-9 | **Medium** | Rate limiting is per-process and resets on restart | `AuthRateLimitGuard` uses an in-memory map | `http/rate-limit.guard.ts` | Single instance | Move counters to Postgres, or document single-instance deployment |
| K-10 | **Low** | A client sending **both** a session cookie and a Bearer token authenticates as the **cookie's** owner | Documented precedence in `auth.guard.ts` | auth | One request context per actor; send `x-csrf-token` | Keep as is; it is deliberate |
| K-11 | **Low** | Scheduled jobs only scan the **default tenant** | Multi-tenant sweeping deferred with OD-1a | `infrastructure/jobs/*` | — | Address when a second tenant exists |
| K-12 | **Low** | The outbox drainer is in-process; multi-node behaviour is untested | Deliberate (decision B7/1.6b) | `infrastructure/outbox/*` | — | Test or move to pg-boss if multi-node is needed |
| K-13 | ~~Low~~ **FIXED** 2026-08-19 | `IssuerMembership.canWorkOnAssets()` had no production caller | Still true after 3.3a–3.3c. The **organisation**-level gate (`IssuerOrganisation.canSubmitAssets()`) is now live in `propose-asset.ts`, but the **per-person** check has no caller because staff, not issuer contributors, still submit every asset | `domain/issuers/issuer-membership.ts:41` | — | Closed by 3.3h: `assertCanWorkOnAssets` is reached whenever an issuer brings an asset, and dropping it fails the stranger's 403 |
| K-14 | **Low** | Draft (unopened) offerings are listed to signed-in investors with a Draft badge and no action | Deliberate at the time, flagged for review | `apps/web` offerings panel | — | Product decision: should unopened inventory be visible? |
| K-15 | **Low** | Distributions paid before the `paid_at` migration have no date and are **excluded** from the income statement | Chosen over showing undated income | `application/portfolio/*` | — | None needed; documented |
| K-16 | **Informational** | Resetting anvil without resetting Postgres leaves `Asset.tokenAddress` values pointing at nothing | Chain and DB are independent | — | Reset both together | — |

## Open questions awaiting the product owner

1. **What exactly may an issuer see about the investors in their offering?** The answer given was
   "all necessary information". A concrete field list must be proposed and approved before
   anything is exposed. (Recommended starting list: holder name, tokens held, allocation date,
   amount invested. Explicitly **not**: national ID, address, bank details, KYC evidence.)
2. **Should an asset be blocked from approval until its rights matrix is established?** It follows
   from the platform's own central claim, but it is a product gate.
3. **Should issuer staff be barred from investing?** An issuer's person holds an
   investor-capable account by construction.
4. **OD-23** — should MFA become mandatory for privileged roles?
5. **OD-7** — which email/SMS provider?
6. **OD-10 / OD-16** — chain governance target and custody hardening.
7. **OD-13** — is the `fa`/RTL locale pack approved?
8. **OD-20** — is there a fee model?

---

## Resolved — kept because the causes are traps

| Issue | Root cause | Fix | Trap to avoid |
|---|---|---|---|
| Chain suite hung ~900 s; CI failed on the **second** KYC approval of a run, never the first | `OnchainidClaimIssuer` held **one** long-lived ethers `NonceManager` for the process while every other adapter took one per call, so a tokenization elsewhere left its counter stale | `LanedOperatorSigner`: a **shared promise lane per account** serialises sends, each send gets its **own** manager | **Do not "simplify" to a shared `NonceManager`** (optimistic allocation ⇒ a dropped send wedges the queue) and **do not force a nonce re-read per send** (ethers' 250 ms RPC cache ⇒ "nonce too low") |
| A CI failure was invisible for six runs | Three compounding causes: job logs need admin rights; Node block-buffers stdout to a file; and `DomainErrorFilter` returned early for `HttpException` **without logging** | Log 5xx that arrive as `HttpException`; `stdbuf -oL -eL` in CI; attach log tails to the **step summary** (which needs no admin rights) | Never assume a 500 is diagnosable — check that it is actually logged |
| `investorVisible`, then `realEstate`/`rights`, were silently dropped on save | The Prisma repository did not map the new fields | Migration + mapping both ways | The shared **repository contract test** is what caught both. Always extend it when adding a field |
| A mutation check polluted the database and the restored run still failed | Mis-wired scoped client wrote rows under the `default` tenant while cleanup keyed on tenants A/B | Key cleanup on the row id prefix | Clean up by a key you control, not by the tenant you assumed |
| Playwright seeded calls ran as the wrong actor | The API prefers the cookie over the Bearer header; the request context had stored a cookie | One `APIRequestContext` per actor + `x-csrf-token` | See K-10 |
| A newly published offering was invisible to the journey test | `REVALIDATE_SECRET` was set nowhere, so the ISR purge had never run | Set it in CI and locally | See K-4 |
| Register-then-navigate aborted the in-flight login | Navigation cancelled the request | Wait for a post-login element | — |
| Ambiguous test selectors (`/address/i` matched two labels; `/2,000,000/` matched `12,000,000`) | Loose regexes | Exact labels/cells | — |
| Duplicated row mapping in a new `PrismaPersonVerification` | Re-implemented the investor mapping | Deleted; composed over `InvestorRepository` instead | One authoritative mapping per concept |
| K-37 | ~~Medium~~ **FIXED** 2026-08-23 | A run on `9231f7a` reported **`cancelled`** after 25 minutes with no failing test — everything before it (lint, typecheck, all unit tests, both builds) had passed. **"Deploy contracts to devnet" hung for 1319 seconds** and spent the job's entire budget | That step was **unbounded**, and `anvil` was backgrounded onto the STEP'S OWN STDOUT rather than a log file. Both are exactly what K-32 diagnosed and fixed for the devnet-RESTART step — the same defect was still sitting in the two steps before it | `.github/workflows/ci.yml` | **Read the step durations first** (`GET /actions/runs/{id}/jobs` is anonymously readable even though job LOGS are 403). A 25-minute `cancelled` is a hung step, not a failing test | Fixed: `timeout-minutes: 5` on the deploy and `2` on the devnet start, anvil's output to `$RUNNER_TEMP/anvil.log`, and a `if: failure()` step that prints that log — because a CI-only failure has to carry its own evidence (K-25). **The lesson is K-22's, written down twice already and broken a third time: every step that talks to the chain needs a bound.** |
| K-38 | ~~High~~ **FIXED** 2026-08-24 | Adding a table that references `investors` with **ON DELETE RESTRICT** breaks every integration suite that wipes investors — and it breaks them ELSEWHERE. `allocation_mints` (P0-2 step 1) surfaced as three `PrismaClientKnownRequestError` failures in `onchainid-claim-issuer`, which has nothing to do with minting, and only in a full run where something upstream had already created a mint row | `investor.deleteMany()` appears unconditionally in five suites. Each new RESTRICT-ing child silently adds a precondition none of them know about. `screening_results` cost 25 failures across 6 files; this one cost five CI cycles | `test/integration/*`, `prisma/schema.prisma` | Clear the children first, in the order `test/support/clear-investors.ts` uses | **Fixed by giving that list ONE owner**: every suite now calls `clearInvestors(prisma)`, so a new child is added in one place. Reproduced deliberately before fixing — seeding a mint row and running `DELETE FROM investors` gives `violates foreign key constraint "allocation_mints_investor_id_fkey"`, the same error Prisma wrapped. **Diagnosing it needed the CI diagnostics built in `3a9de2c`–`f9fccd9`**: job logs are 403, and a suite-level hook failure publishes no failing assertion, so the build was red and silent for four runs |
| K-39 | ~~HIGH~~ **FIXED** 2026-08-24 | **No email was ever delivered.** Password resets, email verifications and important notifications are all ENQUEUED to the transactional outbox (B7), and the drain worker was **opt-in via `OUTBOX_DRAIN_INTERVAL_MS`, which was set nowhere** — not in CI, not in `.env`, not in `launch.json`. Measured on the dev database: **187 pending messages, the oldest six days old** (157 email verifications, 30 notifications). Even the dev sender's `[DEV EMAIL — NOT DELIVERED]` log never appeared, because that runs inside the HANDLER, which only runs on drain | A durability mechanism defaulting to off. Nothing failed loudly — enqueue succeeds, and the queue simply grows | `infrastructure/outbox/outbox-drain-worker.ts` | — | **Fixed by making the worker run by default** (1000ms), keeping the original opt-out as an explicit `=0` which the integration suite sets so it still drives draining deterministically. A nonsense value is treated as OFF and warns, rather than guessing an interval nobody chose. Proven on a live server against real data: the six-day backlog of 187 messages went to `sent` within seconds of startup, all through the dev sender to `@example.com` addresses — no real mail was sent. **Found while starting P0-2 step 2**, which would have enqueued mints into the same undrained queue — the exact failure that reverted the previous attempt |
